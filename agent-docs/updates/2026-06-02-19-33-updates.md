# Updates — May 28, 2026

## 15:11 UTC — Production gate: League of Legends + CS2

- `restrictedMode.ts` — `RESTRICTED_VIDEOGAME_SLUGS` (`cs-go`, `league-of-legends`); tag label helpers; default pill still CS2 when present
- `umbrellaGame.ts` — `isRestrictedProductionUmbrella()`; home/search/GameLinks use allowlist
- Why: enable LoL on prod public surfaces without shipping FIFA odds branch
- Verification: `yarn build:strict`; vitest `restrictedMode.test.ts`

## 00:59 UTC — Production gate: Valorant

- `restrictedMode.ts` — add `valorant` to `RESTRICTED_VIDEOGAME_SLUGS`; `isValorantTagLabel()`
- `restrictedMode.test.ts` — allowlist coverage for Valorant slug and tag label
- Why: show Valorant on prod home/search/GameLinks alongside CS2 and LoL (API already in `ENABLED_GAMES`)
- Verification: `yarn build:strict`

## 19:48 UTC — LevelUp order cancel sends Privy token

- `simplifiedOrderService.cancelOrder` requires `accessToken`; Positions `OrdersView` / `OrdersCardView` pass `getAccessToken()`
- Why: predictions-api `POST /orders/cancel/:orderId` now requires auth and ownership
- Verification: `yarn build:strict`

## 17:47 UTC — Production gate: Dota 2

- `restrictedMode.ts` — add `dota-2` to `RESTRICTED_VIDEOGAME_SLUGS`; `isDota2TagLabel()`
- `restrictedMode.test.ts` — allowlist coverage for Dota 2 slug and tag labels
- Why: show Dota 2 on prod home/search/GameLinks alongside CS2, LoL, and Valorant (API already in `ENABLED_GAMES`)
- Verification: `yarn build:strict`; vitest `restrictedMode.test.ts`

## 20:32 UTC — Remove home pandascore_matchId dedupe

- `FilteredPredictions.tsx` — drop `dedupeUmbrellasByPandascoreMatch` (prod audit: 2568 umbrellas, 0 duplicate matchIds)
- Why: server already 1:1 match→umbrella for moneyline; dedupe masked sidebar vs list mismatch without benefit
- Verification: `yarn build:strict`

## 21:15 UTC — Game pill counts match tagged umbrella list

- `gameLinkFilters.ts` — `umbrellaHasTagId`, `isSpecificGameTagSelection`
- `GameLinks.tsx` — count active umbrellas with tag on any child question
- `FilteredPredictions.tsx` — per-game pill uses flat grid (all tagged); calendar “today+” only for All / Live / Starting Soon
- Why: sidebar already counted tagged umbrellas; list hid past `active` rows via calendar
- Verification: `yarn build:strict`

## 15:06 UTC — Hide ended matches on home catalog

- `gameLinkFilters.ts` — `isUmbrellaEndedForHomeCatalog`, `filterHomeCatalogUmbrellas` (matches PredictionCard live/ended semantics)
- `FilteredPredictions.tsx`, `GameLinks.tsx` — drop ended esports (past 4h live window) and expired daily umbrellas from list + pill counts
- `gameLinkFilters.test.ts` — unit tests for ended/upcoming/live windows
- Why: stale `active: true` past matches polluted prod home when per-game flat grid showed all tagged umbrellas
- Verification: `yarn build:strict`; vitest `gameLinkFilters.test.ts`

## 16:06 UTC — Restore game title on per-game pill view

- `FilteredPredictions.tsx` — `PredictionPageHeading` shared component; flat grid per-game view wraps heading + grid in `prediction-calendar` (same spacing/logo as All view)
- Why: `useCalendarLayout` false for specific game pills dropped `prediction-calendar-page-heading` (regression from 21:15 UTC change)
- Verification: `yarn build:strict`

## 16:15 UTC — Home card team rows: strip tournament from Dota titles

- `historyOutcomeWinner.ts` — `parseVsTeamsFromTitle` stops team B at ` - League` / `(Bo3)` (panda long umbrella format)
- `SingleMarketActions.tsx` — use `resolveOutcomeSideLabels` + pass full `umbrella` from `PredictionCard` (child question / teamMappings first)
- `outcomeSideLabels.test.ts` — long umbrella + child question cases
- Why: naive `vs` split on umbrella displayName put tournament text on team B row (Dota especially visible)
- Verification: `yarn build:strict`; vitest `outcomeSideLabels.test.ts`

## 18:01 UTC — Dynamic OG share previews (nginx + spa-entry manifest)

- `nginx.conf` — proxy `/predictions/umbrella/:id` → API HTML shell; proxy `/og/umbrella/:id.png` → API PNG
- `scripts/spaEntryManifestPlugin.ts` — writes `dist/spa-entry.json` (Vite entry JS/CSS hashes for API HTML shell)
- Why: crawlers get market-specific OG tags on the same URL users copy; SPA still boots underneath
- Verification: `yarn build:strict`

## 19:06 UTC — Umbrella URL syncs odds format for share links

- `oddsFormatQuery.ts`, `useOddsFormatUrlSync.ts` — `?format=` on `/predictions/umbrella/:id` syncs with `OddsDisplayContext`; omitted when default (¢)
- `PredictionMarket.tsx` — mounts URL sync hook
- Why: copied share URL drives OG PNG + in-app venue table odds style
- Verification: `yarn build:strict`; vitest `oddsFormatQuery.test.ts`

## 19:12 UTC — Preserve home filter + scroll on back from umbrella

- `gameLinkFilters.ts` — `resolveStoredHomeGameFilter` / `resolveInitialHomeGameFilter`; gate `setHomeGameFilter` until bootstrap (fixes remount clearing localStorage)
- `homeScrollRestore.ts`, `MainRoutes.tsx` — session scroll save/restore on browser back (`POP` only)
- `FilteredPredictions.tsx` — merged filter bootstrap reads stored pill; unmount saves scroll
- Why: back from umbrella returned to Live + top of page despite prior filter/scroll
- Verification: `yarn build:strict`; vitest `gameLinkFilters.test.ts`, `homeScrollRestore.test.ts`

## 19:25 UTC — Fix home scroll restore race on browser back

- `MainRoutes.tsx` — skip scroll-to-top on `POP` to `/` (restore cleared sessionStorage before parent effect ran)
- `homeScrollRestore.ts` — anchor-based restore picks topmost visible card; retries until alignment stable; CSS attr escape polyfill
- Why: back navigation landed near but not on the prior list position
- Verification: `yarn build:strict`; vitest `homeScrollRestore.test.ts`

- Verification: `yarn build:strict`; vitest `homeScrollRestore.test.ts`

## 19:33 UTC — Simplify home scroll restore to scrollY only

- `homeScrollRestore.ts` — removed anchor card / header offset logic; save `window.scrollY`; `subscribeHomeCatalogScrollSave()` on `scrollend` (+ debounced scroll fallback)
- `FilteredPredictions.tsx` — defer scroll-save subscription until after POP restore; explicit save on navigate kept
- Why: preserve how the user left the page; DOM anchor math was fragile and unnecessary
- Verification: `yarn build:strict`; vitest `homeScrollRestore.test.ts`
