# Updates — May 24, 2026

## 18:45 UTC — Agent-docs cleanup + per-commit update rule

- Restructured `agent-docs/`: slim `BACKLOG.md`, updated `README.md`, added `updates/` for commit logs.
- Added workspace rule `.cursor/rules/agent-docs-commit-updates.mdc` (one dirty `*-updates.md` per commit; append after passing build; rename timestamp on each touch).
- Verification: documentation-only change (no build run for this entry).

## 22:41 UTC — Sortable UTC filenames

- Renamed update log pattern from `may-24-26-18:00-updates.md` to `YYYY-MM-DD-HH-MM-updates.md` (UTC, zero-padded, lexicographic sort).
- Updated `agent-docs-commit-updates.mdc` and `updates/README.md`.
- Verification: documentation-only change (no build run for this entry).

## 22:48 UTC — Move coding-standards into prinx-interface repo

- Moved `Development/coding-standards/` → `prinx-interface/coding-standards/` (was outside any git repo).
- Updated `tsconfig.json` / `tsconfig.node.json` extends paths; `agent-docs/README.md`, `BACKLOG.md`.
- README documents copy/sibling-checkout adoption for `predictions-api` and `Poly-Proxy`.
- Verification: `yarn typecheck`

## 22:49 UTC — Retire BACKLOG.md

- Removed `agent-docs/BACKLOG.md`. Multi-session project tracking moves to update logs + workspace handoffs (e.g. `refactor-2-handoff.md`).
- Final backlog snapshot preserved below (was not previously in this file).
- Updated `agent-docs/README.md` and `agent-docs-commit-updates.mdc`.
- Verification: documentation-only change (no build run for this entry).

### Archived from BACKLOG.md (2026-05-24)

#### Active / planned

| ID                | Status    | Project                              | Scope                                       | Notes                                                                                             |
| ----------------- | --------- | ------------------------------------ | ------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| R2-commit         | `planned` | Commit & deploy refactor-2 tree      | `prinx-interface`, `predictions-api`        | Split commits: trade-box, funding, DFlow pond SOR, Predict complement. Build before `railway up`. |
| R2-wallets        | `planned` | Required funding wallets (fail loud) | Trade box, `accountWallets.ts`, prefund     | No trade until wallet roles exist; partial gates only today.                                      |
| R2-predict-ux     | `planned` | Predict sell success honesty         | `usePredictTradingSession`, SOR leg         | Do not mark filled on HTTP 201 only.                                                              |
| POLY-pos-proxy    | `planned` | Proxy Polymarket positions via API   | `predictions-api`, `usePolymarketPositions` | Today: direct `data-api.polymarket.com` from browser.                                             |
| STANDARDS-rollout | `planned` | Shared coding standards              | `coding-standards/`, other repos            | Copy or sibling-extend from this repo into `predictions-api`, `Poly-Proxy`.                       |

#### Deferred

| ID                        | Status     | Project                            | Why deferred                                                   |
| ------------------------- | ---------- | ---------------------------------- | -------------------------------------------------------------- |
| CASH-rename               | `deferred` | Cash-summary slice rename          | `cash-slices-rename-handoff.md` — after collateral cleanup.    |
| LU-pos-api                | `deferred` | LevelUp positions via server/API   | Need fill consistency proof. See `positions-share-sources.md`. |
| EVM-sign-server-broadcast | `deferred` | EVM sign client → server broadcast | Non-uniform with Privy/Poly relayer; separate pass.            |

#### Completed (reference)

| ID                     | Summary                                                   |
| ---------------------- | --------------------------------------------------------- |
| DRY-pricing            | `orderbookBbo`, `buildVenuePriceRows`, venue adapters     |
| DRY-ensure             | Shared ensure-execution runner; Poly/Predict/Limitless    |
| DRY-panda / DRY-border | `pandaOutcomeSide`, `getBorderColorForSelected`           |
| CHAIN-reads            | Uniform chain reads via `POST /chain/read`                |
| DRY-portfolio-mtm      | Positions summary = MTM source; header = cash + positions |
| DRY-post-bridge        | `resolvePostBridgeMarketBuyWire` in SOR execute           |
| DRY-outcome-labels     | `resolveOutcomeSideLabels`                                |
| DRY-orderbook-bbo      | `bboFromBook`; retired `calculateOrderbookPrices`         |
| AGENT-docs             | This folder + positions policy                            |

## 22:50 UTC — Drop scattered README.md in agent-docs tree

- `agent-docs/README.md` → `agent-docs/guide.md`
- `coding-standards/README.md` → `coding-standards/standards.md`
- Removed `agent-docs/updates/README.md` (merged into `guide.md`)
- Updated `refactor-2-handoff.md` link
- Convention: descriptive lowercase names in `agent-docs/` and `coding-standards/`, not `README.md`
- Verification: documentation-only change (no build run for this entry)

## 23:00 UTC — Canonical architecture.md + remove docs/

- Added `agent-docs/architecture.md` — Mermaid diagrams (provider tree, per-user data, SOR, VACM, module map); merged `docs/USER_DATA_FLOW.md` + refactor-2 overview
- Added `.cursor/rules/agent-docs-architecture.mdc`; wired into `agent-docs-commit-updates.mdc` and `guide.md`
- Deleted entire `prinx-interface/docs/` (stale quick-reference, architecture.md, HTML exports)
- Updated links in `positions-share-sources.md`, `refactor-2-handoff.md`, workspace `refactor-2-summary.md`
- Verification: documentation-only change (no build run for this entry)

## 18:08 UTC — Prettier + unified lint/format toolchain

- Added Prettier (`coding-standards/prettier.config.cjs`, `format` / `format:check` scripts)
- `eslint-config-prettier` in `.eslintrc.cjs`; `build:strict` includes `format:check`
- ESLint cleanup: stale disable comments, `StreamEmbed` hooks order, relaxed noisy rules
- Verification: `yarn build:strict`

## 18:15 UTC — Master data-flow diagram in architecture.md

- Added §11 master node diagram (sources → stores → UI + trade path) at bottom of `agent-docs/architecture.md`
- Verification: documentation-only change (no build run for this entry)

## 18:22 UTC — Rendered HTML architecture diagram

- Added `agent-docs/architecture-diagram.html` + `diagrams/master-data-flow.mmd` (Mermaid renders in browser; Cursor MD preview shows plain text)
- Linked from `architecture.md` §11 and `guide.md`
- Verification: documentation-only change (no build run for this entry)

## 14:36 UTC — Architecture rule: sync .mmd + .md + .html as one update

- Updated `agent-docs-architecture.mdc`: master diagram trio must stay in sync; one changelog entry, not three
- Updated `agent-docs-commit-updates.mdc`, `architecture.md` header, `.mmd` / `.html` sync comments
- Verification: documentation-only change (no build run for this entry)

## 14:44 UTC — Master diagram: LevelUp shares in AccountDataContext

- Synced `diagrams/master-data-flow.mmd`, `architecture-diagram.html`, `architecture.md` §11 (one pass): removed `UserDataContext` / `levelupStore` trading node; added `useLevelUpPositions` → `AccountDataContext.positions.levelup` with Base RPC + catalog from `PredictionDataContext`
- Updated `architecture.md` §2–§4 provider tree, data-ownership table, per-user flow diagram, import reference
- Why: diagram matched refactor — LevelUp shares already live in `AccountDataContext`; `UserDataProvider` is Mixpanel identify only
- Verification: documentation-only change (no build run for this entry)

## 14:49 UTC — Drop architecture-diagram.html; Mermaid extensions

- Deleted `agent-docs/architecture-diagram.html`; master graph is `.mmd` + `architecture.md` §11 only
- Added `prinx-interface/.vscode/extensions.json` (`bierner.markdown-mermaid`, `vstirbu.vscode-mermaid-preview`)
- Updated `guide.md`, `architecture.md`, `master-data-flow.mmd` header; workspace rules `agent-docs-architecture.mdc`, `agent-docs-commit-updates.mdc` (pair not trio)
- Why: preview in Cursor via extensions instead of duplicate HTML
- Verification: documentation-only change (no build run for this entry)

## 15:14 UTC — Master diagram: Polymarket positions via private REST only

- Synced `diagrams/master-data-flow.mmd` + `architecture.md` §11: removed `Polymarket Data API` from external sources and dashed “server proxy” edges; `GET /api/polymarket/positions` and `/activity` draw like Predict/Limitless (`API → route → AccountDataContext`)
- Clarified §11 read order: external sources = what the client calls directly
- Why: Polymarket portfolio reads are proxied through predictions-api; upstream Data API is not a client-visible source
- Verification: documentation-only change (no build run for this entry)

## 15:25 UTC — LevelUp positions via GET /api/levelup/positions

- **predictions-api:** `GET /api/levelup/positions` — Privy auth, resolve Base smart wallet, Mongo `Question` catalog, batch CTF `balanceOf` via `BASE_RPC_URL` (`domain/levelup/`)
- **prinx-interface:** `useLevelUpPositions` → TanStack + `privateApi.getLevelUpPositions()`; post-trade invalidates `levelUpQueryKeys.positions`
- **Docs:** master diagram removes `Base RPC` from external sources; all venue position routes now via `predictions-api private REST`
- Why: uniform client fetch path across venues (same as Predict/Limitless/DFlow/Polymarket)
- Verification: predictions-api `yarn typecheck`; prinx-interface `yarn typecheck`; `npx vitest run src/features/trading/sor/tests/performPostTradeDataRefresh.test.ts`

## 15:29 UTC — LevelUp approval reads via POST /chain/read

- **Client:** `useLevelUpApprovalsStatus` + `ensureLevelUpApprovals` use `fetchLevelUpApprovalsChainRead` (`levelUpChainRead.ts`); removed client Base RPC from `levelUpApprovalAdapter.ts`
- **Docs:** `architecture.md` import table
- Why: uniform approval read path with other venues
- Verification: `yarn typecheck`

## 15:33 UTC — LevelUp post-trade scoped RPC refresh (client)

- `privateApi.refreshLevelUpPositions`, `refreshLevelUpPositionsByTokenIds`, `mergeLevelUpPositionRows`
- `AccountDataContext.refresh.levelUpPositionsByTokenIds`; post-trade registry uses tokenIds from filled route (`levelUpTokenIdsFromFilledRoute`)
- `useTradeBoxSorExecution` passes `levelUpRefreshTokenIds`; claims use catalog yes/no mints (`useHandleClaimSuccess` → `useLevelUpPortfolioRefetch`)
- Master diagram + `architecture.md` §2 / §11
- Why: 1–2 RPC `balanceOf` after fill instead of full subgraph refetch each poll
- Verification: `npx vitest run` merge + post-trade tests; `yarn typecheck` passes for changed files (full `yarn build:strict` still blocked by `src/App` vs `src/app` casing on disk)

## 15:52 UTC — guide.md Mermaid setup for humans and agents

- Expanded `agent-docs/guide.md`: required Mermaid extensions (install, verify, CLI), agent instructions when diagrams won't render, fixed stale `updates/` link
- Why: co-founder and agents need explicit setup before `architecture.md` / `.mmd` are usable in Cursor
- Verification: `npx prettier --write agent-docs/guide.md`

## 16:38 UTC — LevelUp first-trade approval fixes

- `levelUpApprovalAdapter.ts` — require Privy `smart_wallet` linked account; assert SCW matches VACM wallet; poll server read after approve tx; log incomplete flags
- `addresses.ts` — clarify testnet-only legacy CTF in TESTNET block
- Why: first-time LevelUp users could approve via wrong wallet path or fail re-read before server RPC caught up
- Verification: `npx vitest run src/features/trading/venues/levelup`

## 16:50 UTC — Remove legacy SCW → Exchange USDC approval

- `levelUpApprovalAdapter.ts`, `chainReadTypes.ts`, `useVenueTokenApprovals.ts` — batched approve is CTF + FeeWrapper only (2 calls); drop `hasUsdcApproval`
- Why: server was checking Exchange USDC allowance while client only needed FeeWrapper path
- Verification: typecheck on changed files (full `yarn build:strict` blocked by pre-existing App/app casing)

## 16:56 UTC — Revert FeeWrapper-only USDC approval change

- Restored 3-call SCW batch and `hasUsdcApproval` gate to match pre-refactor `main`
- Verification: typecheck on changed files

## 16:56 UTC — Brand theme purple → blue (#0478FF)

- Added `src/styles/themes/scss/_colors.scss` and `src/styles/themes/colors.css` as the single source of truth for brand accent tokens (`--brand-primary` and hover/active/light variants).
- Wired theme into `globals.css` (Privy accent vars), Vite SCSS `additionalData`, `Shared.scss` button tokens, and replaced hardcoded purple hex across SCSS/TSX with CSS variables.
- Verification: `yarn build:strict`

## 16:59 UTC — Remove LevelUp testnet env; localhost uses production contracts

- `environment.ts`, `addresses.ts`, `predictionApiBase.ts`, `privateApiBase.ts`, `dev-prompt.ts`, `package.json`, `.env` — `local` + production Base addresses; localhost → :8080 API
- Why: client/server approval mismatch when localhost picked testnet contracts but local API used production env
- Verification: `yarn typecheck` (pre-existing App/app casing only)

## 17:00 UTC — Brand blue tints + inactive surfaces

- Extended theme tokens with `--brand-tint-*`, `--brand-surface-inactive`, and `--brand-gradient-selected` for highlights that were still purple rgba.
- Trade box: selected venue rows, empty amount fields, and menu hovers now use blue tints; copy buttons on Transfers use brand tints.
- Positions tab/expand controls use blue highlight backgrounds instead of purple rgba.
- Verification: `yarn build:strict`

## 17:13 UTC — About page CTA button text legibility

- Global link styles (`#646cff`) were painting blue text on primary `<Button>` links; excluded `.button` from global anchor rules and pinned primary button label color to white.
- Added `--color-white` token; inline links now use brand blue instead of legacy purple-indigo.
- Verification: `yarn build:strict`

## 17:21 UTC — Shared outline buttons + hover ring fix

- Added `themes/outline-button.css` + `_outline-button.scss` mixin; Profile Edit/Save and Transfers Copy share one blue outline/hover/focus treatment.
- Fixed global `button:hover` purple border (`#646cff` → `--brand-primary`).
- About CTAs use `brand-outline-button` (compact outline style) instead of oversized solid primary buttons.
- Verification: `yarn build:strict`

## 18:23 UTC — ClutchComet link-preview and site metadata

- Added `src/config/siteMetadata.ts` (canonical origin `https://clutchcoment.com`, title/description, `@Clutch_Comet`).
- Rewrote `index.html` OG/Twitter tags; fixed absolute image URLs; added `public/favicon.png` and `public/og-image.png`.
- Replaced legacy Prinx/LevelUp strings in `SEO.tsx`; mount default `<SEO>` on `App`; Privy login message → ClutchComet.
- Verification: `yarn build:strict`

## 18:29 UTC — Metadata audit (no em dashes, single source)

- Title uses pipe separator: `ClutchComet | Prediction market aggregator` (no em dashes).
- `scripts/viteSiteMetadataHtml.ts` injects `siteMetadata.ts` into `index.html` so static shell and Helmet cannot drift.
- OG image dimensions match `public/og-image.png` (969×461); added `og:image:alt` / `twitter:image:alt`.
- Verification: `yarn build:strict`

## 18:33 UTC — ClutchComet brand logo sitewide

- `src/assets/brandLogo.ts` + `ClutchCometColored.png` on header, chart header (above Recharts), and footer.
- Regenerated `public/favicon.png` (180px) and `public/og-image.png` (1200×630, black pad) for link previews; updated `siteMetadata` OG dimensions.
- Verification: `yarn build:strict`

## 18:42 UTC — Rounded favicon corners

- Regenerated `public/favicon.png` from `ClutchCometColored.png` with ~22% corner radius (180×180).
- Verification: asset-only (no build rerun)

## 19:27 UTC — Onboarding speed (refactor-2 client)

- Checked out `refactor-2` on client (`e120f9897`); server left on `refactor-2` (`3c012ae`), no server edits (P2 skipped).
- **P0:** Ported refetch-stability hunks from `403c282` — stable `.refetch` deps in `AccountDataContext`, LevelUp portfolio/approval hooks, removed mount-time LevelUp approval refetch in `useTradeBoxApprovals`.
- **P1:** `deferPortfolioBoot` while `!profile.onboardingCompletedAt` — disables all venue position queries and skips position refetch in `refreshAccount` / `refreshAll` during first-signup modal.
- **Build fix:** Normalized `@/App/` imports to match git-tracked `src/App/` casing (refactor-2 had mixed `app`/`App` paths breaking `yarn build:strict` on case-sensitive TS).
- Verification: `yarn build:strict` (client). Manual signup timing: use `VITE_DEBUG_TRADING=true` + Network tab on fresh user for before/after position call counts.

## 19:07 UTC — Merge origin/refactor-2 with ClutchComet brand commit

- Resolved rename/rename conflict on agent-docs update logs; combined remote LevelUp/env changes with local ClutchComet branding entries.
- Verification: merge commit (build not rerun for log-only resolution)

## 20:02 UTC — Revert trade box empty input blue tint

- Removed `--brand-surface-inactive` from empty amount/limit fields; restored `#101114` / `#4b5563` (mobile) and frosted `rgba(0,0,0,0.34)` (desktop).
- Verification: SCSS-only trade-box revert (workspace build blocked by unrelated App/app casing TS1261)

## 22:49 UTC — Home filter includes non-esports tagged markets (FIFA)

- `gameLinkFilters.ts` — `umbrellaMatchesHomeFilterType()` / `readUmbrellaChildTagState()`; `filterType="all"` includes any tagged umbrella (esports + FIFA World Cup)
- `FilteredPredictions.tsx`, `GameLinks.tsx` — shared filter helper for base pool + sidebar pill counts
- Why: FIFA questions use `fifa-wc-2026` tag without ESPORTS; old logic hid them on `/`
- Verification: changed files lint-clean; full `yarn build:strict` blocked by pre-existing `privateApiBase.ts` unused `isLocalApi`

## 23:10 UTC — Bundled FIFA World Cup logo (fifa-26-logo.png)

- Added `src/assets/game-logos/fifa-26-logo.png` (from Desktop); `gameLogoResolver.ts` maps slug `fifa-wc-2026` / label `FIFA World Cup` like CS2 bundled logos
- `GameLinks`, `PredictionCard`, `MarketHeader`, `FilteredPredictions` — prefer bundled FIFA logo over `Tag.imageUrl`
- Why: host emblem in Vite bundle instead of remote Wikimedia / Mongo URL
- Verification: `yarn typecheck` on changed files (full build still blocked by pre-existing `privateApiBase.ts`)

## 01:42 UTC — Client step 2: polymarket odds subscribe key

- `resolveOddsSubscriptionKey.ts` — esports → `umbrella.pandascore_matchId`; `source: "polymarket"` → active question `polymarketMarketId`; per-leg Limitless routing via `resolveLimitlessRoutingForOdds`
- `useUmbrellaTradePricing.ts`, `MarketPanels.tsx`, `HomeInlineTradeLayout.tsx` — pass active moneyline leg; wire key drives venue-prices subscribe + cross-venue panels/chart
- Types: `Umbrella.source`, `PredictionMarket.polymarketMarketId` / `exchangeMatching`
- Why: FIFA mirror umbrellas have no Panda match id; server step 1 keys matched-markets rows by `polymarketMarketId`
- Verification: `yarn build:strict`; vitest `resolveOddsSubscriptionKey.test.ts`

## 01:55 UTC — FIFA 3-way cross-venue Basic tab (home | draw | away)

- `resolveFifaThreeWayOddsContext.ts` — 3 leg subscribe keys + column labels from `moneylineLeg` + `teamMappings`
- `buildFifaThreeWayVenuePriceRows.ts`, `useFifaThreeWayTradingPagePrices.ts` — merge per-leg matched rows; each cell = that leg's YES ask
- `EsportsVenueBooksPanel` — 3-column table when `layout: "threeWay"`; trade box still uses active leg via `activeLegTradingPagePrices`
- Why: esports binary Team A/B columns showed draw YES/NO (22¢/79¢) under Mexico/South Africa headers
- Verification: `yarn build:strict`; vitest `buildFifaThreeWayVenuePriceRows.test.ts`, `resolveFifaThreeWayOddsContext.test.ts`
