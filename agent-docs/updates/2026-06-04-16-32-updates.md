# Updates — Jun 3, 2026

## 15:13 UTC — Fix umbrella odds format menu vs ?format= URL sync

- `useOddsFormatUrlSync.ts` — URL→context effect runs only on `searchParams` change (not `oddsDisplayStyle`), so menu clicks are not reverted by stale `?format=`
- `tests/useOddsFormatUrlSync.test.tsx` — regression: american URL → menu picks decimal → context + URL stay decimal
- Why: OG share-link sync raced context→URL and blocked trade widget odds format changes on umbrella page
- Verification: `yarn build:strict`; vitest `useOddsFormatUrlSync.test.tsx`

## 02:07 UTC — Panda esports: moneyline-only in catalog UI

- `pandaMoneylineQuestion.ts` — `isPandaMoneylineQuestion`, `filterPandaMoneylineQuestions`, `pickPandaMoneylineQuestion` (match vs game `winner-2-way`)
- `PredictionDataContext.tsx` — display `children` filtered for `pandascore_matchId` umbrellas; `originalChildren` + `allMarketsByUmbrella` unchanged
- `esportsHomeCard.ts`, `PredictionMarket/utils.ts` — pickers use match-level row when map legs present
- Tests: `pandaMoneylineQuestion.test.ts`, `esportsHomeCard.test.ts`
- Why: hide map-level questions in production UI while map markets are built locally on `maps` branch
- Verification: `yarn build:strict`

## 04:09 UTC — Remove 52-match venue-prices subscription cap

- `context/VenuePandaSubscriptionContext.tsx` — deleted `MAX_VENUE_PANDA_SUBSCRIPTIONS` (was 52); `flushActive` now subscribes every match the UI references (`countsRef.keys()`), no `slice`/priority sort
- `pages/Predictions/components/FilteredPredictions.tsx` — removed the `break` at the cap and the now-unused import; `visiblePandaIdsForVenueWs` lists all visible matches
- Why: the 52 cap was a front-end-only guard; the server maintains all books and forwards per client subscription. With CS (19) + other games + World Cup the board exceeded 52, so the overflow (a chunk of CS) silently got no odds. Server has no such cap.
- Behavior: per-client subscribe set now follows what the UI displays (still targeted, not `subscribe_all`). Follow-up later: scope to virtualized/rendered rows; optional BBO-only payload for large boards.
- Verification: `yarn build:strict` (pass)

## 16:14 UTC — Let Panda map questions flow into the UI (remove series-only display filter)

- `context/PredictionDataContext.tsx` — removed `filterPandaMoneylineQuestions(openMarkets, umbrella)`; display `children` = all open (non-resolved) questions, so map `winner-2-way` legs flow through alongside the match moneyline. `originalChildren` still = raw markets.
- Home cards still resolve the match-level row via `pickPandaMoneylineQuestion` (`esportsHomeCard.ts`, `PredictionMarket/utils.ts`) — unchanged. The detail view now receives series + map questions.
- Side effect: Panda matches with maps now have `children.length > 1`, so they route through the multi-market path instead of single. Map-tab/design work to follow.
- `filterPandaMoneylineQuestions` retained (still covered by `pandaMoneylineQuestion.test.ts`) but no longer used in the catalog data path.
- Why: build the map front end; design to be figured out from here.
- Verification: `yarn build:strict` (pass)

## 16:32 UTC — Umbrella card: stacked per-question odds (series + each map)

- `features/markets/presentation/pandaOddsRows.ts` (new) — `buildPandaOddsRowSpecs(matchId, questions)` → ordered rows (Series Winner, Map 1..N) with venue-prices wire keys (`matchId`, `matchId-map-N`); excludes over/under; only rows that exist. `pandaVenueWireKeys` helper for subscriptions.
- `pages/Predictions/components/EsportsMatchMapOddsRows.tsx` (+ `.scss`, new) — stacked labeled rows; each row resolves its matched market by **strict wire key** (no umbrellaId fallback) → `listingBestYesNoFromMatched`; click → umbrella.
- `pages/Predictions/components/PredictionCard.tsx` — in the esports match-winner branch, when `esportsOddsRowSpecs.length > 1` render the stacked rows; single series row keeps the existing `SingleMarketActions` card. No change for non-map / non-esports cards.
- `pages/Home/Home.tsx`, `pages/Predictions/components/FilteredPredictions.tsx` — venue-prices subscription now subscribes **all** wire keys per umbrella (series + maps) via `pandaVenueWireKeys`, not just the bare `pandascore_matchId`. Server only streams subscribed keys, so this is required for map odds to arrive.
- Caveat: map rows are LevelUp-only on localhost until venue sharding lands + `STREAM_MAP_QUESTION_VENUES` temp flip reverted.
- Verification: `yarn build:strict` (pass)
